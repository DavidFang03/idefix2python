[project]
name = "idefix2python"
version = "0.1.0"
dependencies = [
  "inifix>=5.1.0",
  "matplotlib",
  "numpy",
  "scipy",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
